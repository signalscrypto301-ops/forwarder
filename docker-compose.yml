networks:
  app-network:
    driver: bridge
services:
  whatsapp-bot:
    build: ./Whatsapp
    ports:
      - "127.0.0.1:5426:5426"
    environment:
      - API_SECRET=forwarder_internal_secret_key_8f3a92b
      - PORT=5426
      - CLIENT_ID=user
      - ACCOUNT_1_PROXY=${ACCOUNT_1_PROXY:-}
      - ACCOUNT_2_PROXY=${ACCOUNT_2_PROXY:-}
      - ACCOUNT_3_PROXY=${ACCOUNT_3_PROXY:-}
      - ACCOUNT_4_PROXY=${ACCOUNT_4_PROXY:-}
    volumes:
      - ./Whatsapp/.baileys_auth:/app/.baileys_auth
      - ./Whatsapp/uploads:/app/uploads
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://localhost:5426/health?live=1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
    restart: unless-stopped
    networks:
      - app-network
  telegram-bot:
    build: ./Telegram
    environment:
      - API_SECRET=forwarder_internal_secret_key_8f3a92b
      - WHATSAPP_SERVICE=http://whatsapp-bot:5426
    volumes:
      - ./Telegram:/app
    restart: unless-stopped
    depends_on:
      whatsapp-bot:
        condition: service_healthy
    command: sh -c "python bot.py"
    networks:
      - app-network

